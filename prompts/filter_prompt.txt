Rate how helpful each historical event is for answering the question.

Question:
{query}

Historical events:
{history}

Instructions:
- Output ONLY a JSON array of exactly {n} numbers (one per event above).
- Each number is a helpfulness score from 0.0 to 1.0.
- 1.0 = very helpful, 0.0 = not helpful.
- Spread the scores - do NOT give all zeros or all ones.
- Example for 3 events: [0.8, 0.3, 0.6]

Output ONLY the JSON array, nothing else:
