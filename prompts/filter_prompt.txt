There is a question and some historical events. Please select the historical event that is most helpful in answering this question from these historical events and return the number before the event.

Question:
{query}

Historical events:
{history}

Your choice is:

IMPORTANT (for this codebase):
- Output ONLY a JSON array of exactly {n} floats.
- The i-th float corresponds to the i-th historical event in the order shown above.
- Larger float means the historical event is more helpful for answering the question.
- Do NOT output all zeros; even if uncertain, add small differences between values.
- Output ONLY the JSON array. No extra text.
