Main target query (the masked prediction you will make later in the pipeline; keep dates exactly as given):
{target_query}

Below is an analogical replay block: a time-ordered chain of historical events, then a masked question about a similar past event, and its known answer (from history). Explain how the known answer is supported by the chain (paper §3.3 analogical reasoning).

Historical Events:
{history}

Question (similar past event, masked object):
{question}

Answer (known object for that similar past event):
{answer}

Your Analysis:
